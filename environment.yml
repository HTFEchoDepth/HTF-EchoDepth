# HTF-EchoDepth conda environment entry point.
# See docs/ENVIRONMENT.md for the reference verification environment.

name: htf-echodepth
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - -r requirements.txt
