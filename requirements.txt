# HTF-EchoDepth — minimal runtime deps (Phase 2 placeholder)
# Pin versions in Phase 3 after code extraction and smoke tests.

torch>=2.0
torchaudio>=2.0
torchvision>=0.15
numpy>=1.23
pandas>=1.5
hydra-core>=1.3
omegaconf>=2.3
librosa>=0.10
soundfile>=0.12
Pillow>=9.0
matplotlib>=3.7
tqdm>=4.65
PyYAML>=6.0
